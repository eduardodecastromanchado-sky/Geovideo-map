name: Build

on:
  push:
    branches:
      - main

jobs:
  backend:
    name: Backend - Build and analyze
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9
        with:
          java-version: "17"
          distribution: zulu

      - name: Cache SonarQube packages
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache Maven packages
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-k**s: ${{ runner.os }}-m2

      - n**e: Build and analyze backend
    **  working-directory: ./backend
  **    env:
          SONAR_TOKEN: $** secrets.SONAR_TOKEN }}
         **ONAR_HOST_URL: ${{ secrets.SONAR_**ST_URL }}
          REMEMBER_ME_K**: sonar-test-remember-me-key
    **  run: >
          mvn -B verify
**        -DskipTests
          org**onarsource.scanner.maven:sonar-ma**n-plugin:5.8.0.7211:sonar
       ** -Dsonar.projectKey=eduardodecast**manchado-sky_Geovideo-map_3d19bc6**89da-4414-be50-a7677b1f3414
     **   -Dsonar.projectName=Geovideo-m**

  frontend:
    name: Frontend **Build and analyze
    runs-on: se**-hosted

    steps:
      - name:**heckout
        uses: actions/che**out@34e114876b0b11c390a56381ad16e**13914f8d5
        with:
         **etch-depth: 0

      - name: Set ** Node.js
        uses: actions/se**p-node@v6
        with:
         **ode-version: "22"
          cache**npm
          cache-dependency-pa**: frontend/package-lock.json

   ** - name: Install frontend depende**ies
        working-directory: ./**ontend
        run: npm ci

     ** name: Build frontend
        wor**ng-directory: ./frontend
        **n: npm run build

      - name: A**lyze frontend
        uses: Sonar**urce/sonarqube-scan-action@7006c4**2b2e0ee0f816d36501671557c97f5995
**      with:
          projectBase**r: frontend
          args: >
   **       -Dsonar.projectKey=eduardo**castromanchado-sky_Geovideo-map-f**ntend
            -Dsonar.project**me=Geovideo-map-frontend
        **  -Dsonar.sources=src
        env**          SONAR_TOKEN: ${{ secret**SONAR_TOKEN }}
          SONAR_HO**_URL: ${{ secrets.SONAR_HOST_URL **
